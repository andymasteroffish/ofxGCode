ofxGCode Updates

[X] remove fbo
[] have everything represented by GLine
[] standardize the language around “clip inside”
[] add hershey font
[] size independent from window size

Stretch:
[] be able to tag lines so you can specify which ones to be removed in a culling


GLine Overhaul
[] do things like last_x do antyhing?
[] remove GCodePoint
[] remove pressure and speed
[] test dots
[] make sure sorting works
[] do you need to store vector<string> commands or just generate in the make gcode function?
[] work on simplify() or remove it
[] draw transit lines
[] clean up and remove commented out old code
[] measureTransitDistance()
[] clip_inside()
	-needs to handle lines that pass through
[] set_outwards_only_bounds()


Things to preserve:
[] do not reverse (should be in gline)
[] clipping to ensure you do not go out of range